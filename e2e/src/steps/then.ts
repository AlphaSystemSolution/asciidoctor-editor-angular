/// <reference path="../../../typings/index.d.ts"/>

import { StepDefinitions } from 'cucumber';

module.exports = function then() {
    let step: StepDefinitions = this;
    // step.Then(/^I open the (url|site) "([^"]*)?"$/, openWebsite);
};
